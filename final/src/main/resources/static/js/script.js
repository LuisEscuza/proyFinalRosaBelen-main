function ingresoEstadisticas(){
  // Evita que se envíe el formulario al hacer clic en el botón
  event.preventDefault();
  // Redirige al usuario a la página del panel de control
  window.location.href = "http://localhost:8080/estadisticas/";
}
function ingresoProductos(){
  // Evita que se envíe el formulario al hacer clic en el botón
  event.preventDefault();
  // Redirige al usuario a la página del panel de control
  window.location.href = "http://localhost:8080/producto/";
}
function ingresoClientes(){
  // Evita que se envíe el formulario al hacer clic en el botón
  event.preventDefault();
  // Redirige al usuario a la página del panel de control
  window.location.href = "http://localhost:8080/clientes/";
}
function ingresoCuenta(){
  // Evita que se envíe el formulario al hacer clic en el botón
  event.preventDefault();
  // Redirige al usuario a la página del panel de control
  window.location.href = "http://localhost:8080/cuenta/";
}
function ingresoPedidos(){
  // Evita que se envíe el formulario al hacer clic en el botón
  event.preventDefault();
  // Redirige al usuario a la página del panel de control
  window.location.href = "http://localhost:8080/pedidos/";
}
function ingresoCategoria(){
  // Evita que se envíe el formulario al hacer clic en el botón
  event.preventDefault();
  // Redirige al usuario a la página del panel de control
  window.location.href = "http://localhost:8080/categoria/";
}